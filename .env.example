# Deployment Configuration
# Copy this file to .env.local and modify as needed

# Option 1: Use DEPLOY_TARGET (recommended)
# Set to 'custom-domain' for custom domain deployment (stillnesspower.com)
# Leave unset or set to 'github-pages' for GitHub Pages deployment
DEPLOY_TARGET=github-pages

# Option 2: Use PUBLIC_BASE_PATH (overrides DEPLOY_TARGET)
# Set the exact base path for your deployment
# Examples:
# PUBLIC_BASE_PATH=""                    # For custom domain (stillnesspower.com)
# PUBLIC_BASE_PATH="/stillness-power"    # For GitHub Pages (seth-lupo.github.io/stillness-power)
# PUBLIC_BASE_PATH="/my-new-repo"        # For different GitHub Pages repo
# PUBLIC_BASE_PATH=""